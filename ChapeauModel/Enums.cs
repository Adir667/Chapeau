
public enum OrderItemStatus { Waiting, Ready, Served }

public enum JobPosition { Waiter, Bartender, Chef, Owner }

public enum PaymentMethod { Cash, DebitCard, CreditCard }

public enum TableStatus {Free, IsRunning, FoodIsReady}

